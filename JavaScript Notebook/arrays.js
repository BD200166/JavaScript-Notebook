// Javascript arrays

//Index         0      1      2         //var/let initializes a variable
var colors = ["red","green","blue"]     //An array is initializes with the use of []
//console.log(colors[0])                //Arrays can contain multiple contents of different data types
                                        

//console.log(colors.length) <- ".length" returns the number of elements in the array             

/*
colors.push("purple")  <- ".push" adds another element to the end of the array
console.log(colors)
*/

/*
colors.pop()  <- ".pop" removes the element at the end of the array
console.log(colors)
*/

/*
colors[0] = "yellow"  <- The content of the array/element of the respective index is changed/rewritten                                     
console.log(colors)
*/